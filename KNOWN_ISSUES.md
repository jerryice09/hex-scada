# 실제 구현 시 문제가 될 수 있는 지점과 개선 방향

이 문서는 현재 코드(브라우저 기반 시뮬레이션 UI)를 실제 열교환기 공정에
그대로 투입한다고 가정했을 때 드러날 수 있는 문제를 정리한 것이다.
일부(★ 표시)는 이번 리팩터링에서 코드로 직접 수정했고, 나머지는
아키텍처 변경이 필요해 이 문서에 권장사항으로만 남겨둔다.

## 1. 이번에 코드로 수정한 항목 (★)

| 문제 | 원인 | 수정 내용 |
|---|---|---|
| ★ SOP 팝업이 먼저 발생한 순서대로만 표시됨 | `sopQueue`가 단순 FIFO 배열이라, 나중에 발생한 더 위급한 사고(가스누출)가 먼저 발생한 덜 위급한 사고(열교환이상) 뒤로 밀림 | `sopQueue`를 `{id, priority}` 객체 배열로 바꾸고 push 시점에 우선순위로 재정렬 (`App.jsx`) |
| ★ 사고 ID가 `Date.now()` 기반이라 이론상 충돌 가능 | 같은 밀리초에 같은 코드가 재발생하면 ID 중복 위험 | `incidentSeq` ref로 순번 기반 ID(`INC-1-GAS_LEAK`) 발급 |
| ★ 사고 이력이 무한정 누적됨 | 24시간 연속 운전 시 배열이 계속 커져 브라우저 메모리/렌더링 성능 저하 | `MAX_INCIDENT_HISTORY(300건)`로 최근 항목만 유지 |
| ★ 임계값 저장 시 입력값 검증이 없음 | 하한 ≥ 상한, 빈 값, 음수 압력/유량/가스 등도 그대로 저장되어 판단 로직이 깨질 수 있음 | `validateThresholds()`로 저장 전 검증, 실패 시 에러 토스트 + 미적용 |
| ★ 센서 오류(FAULT) 판정 상한이 완전히 고정값 | 사용자가 설정 탭에서 정상범위를 크게 올리면(예: 압력 900kPa 공정) 실제 정상값이 FAULT로 오탐될 수 있음 | `getEffectiveFaultBounds()`가 "물리적 절대 한계"와 "현재 임계값×1.5" 중 큰 값을 사용하도록 동적화 |

## 2. 구조적으로 더 큰 변경이 필요한 항목 (권장사항)

### 2.1 하드웨어 연동
- 지금은 `jitter()`로 값을 흉내 낼 뿐, 실제 아두이노(G1/2 Brass, YF-S201 등)와 연결되어 있지 않다.
- 브라우저가 USB 시리얼을 직접 읽으려면 **Web Serial API**(Chrome/Edge, HTTPS 필수)를 쓰거나,
  아두이노 → (Python/Node 백엔드, 시리얼 폴링) → WebSocket/MQTT → 브라우저 순으로 중계하는
  별도 게이트웨이가 필요하다. 지금 구조에는 이 계층이 통째로 빠져 있다.

### 2.2 타이머 드리프트 & 백그라운드 탭
- `setInterval`을 3곳(지터, 히스토리, 시계)에서 독립적으로 돌리고 있어 시간이 지날수록
  서로 미세하게 어긋난다(JS 이벤트 루프 지연 누적).
- 더 치명적인 문제는, 브라우저가 백그라운드 탭의 `setInterval`을 초당 1회 이하로
  강제 스로틀링한다는 점이다. 즉 **운전자가 다른 탭을 보고 있으면 감지 주기가 느려지거나
  멈출 수 있다.** 안전 감시 시스템이 탭 포커스에 의존해서는 안 된다.
- 개선 방향: 판단 로직 자체는 서버(백엔드)에서 돌리고, 브라우저는 결과를 구독만 하는
  구조로 바꿔야 탭 비활성화와 무관하게 안전하게 동작한다.

### 2.3 소프트웨어 단일 장애점(SPOF)
- 이 UI가 유일한 안전장치가 되어서는 안 된다. 브라우저 크래시, 노트북 절전모드,
  네트워크 단절 시에도 실제 릴리프 밸브/차단 밸브 같은 **물리적 인터락은 소프트웨어와
  독립적으로 동작**해야 한다. 현재 구조는 "모니터링·경보·SOP 안내" 계층으로만 써야 하며,
  실제 밸브 제어는 PLC 등 별도의 하드웨어 안전 회로가 담당해야 한다.

### 2.4 다중 사용자/다중 화면 동기화
- 상태가 전부 React 로컬 state이므로, 같은 시스템을 다른 브라우저 탭이나 다른 운영자가
  동시에 열면 서로 다른 사고 이력·인지 상태를 보게 된다.
- 실제 현장에서는 여러 운영자가 동시에 볼 수 있어야 하므로 백엔드(DB) + WebSocket 기반의
  중앙 상태 동기화가 필요하다.

### 2.5 감사 추적(Audit Trail) 부재
- "인지 완료"/"조치 완료" 버튼에 로그인·운영자 식별 정보가 없다. 산업안전보건법 등
  규정 준수를 위해서는 **누가, 언제 확인/조치했는지**가 남아야 하는데 지금은 익명 클릭만 기록된다.
- 임계값을 누구나 제한 없이 바꿀 수 있는 것도 문제다. 안전 임계값 변경에는 최소한
  권한 분리(운영자/관리자) + 변경 이력 로그가 필요하다.

### 2.6 센서 오류 탐지의 한계
- 지금 방식은 "물리적으로 말이 안 되는 값"만 걸러낸다. 실제로는 고장난 센서가
  그럴듯하지만 틀린 값(예: 특정 값에 고정되는 stuck-at 고장, 통신 끊김)을 낼 수 있는데
  이런 경우는 지금 로직으로 잡히지 않는다.
- 개선 방향: 통신 타임아웃 감지, 값이 일정 시간 이상 전혀 변하지 않는 "고착(stuck)" 감지,
  이중화 센서 간 상호 비교(voting) 등을 추가로 고려해야 한다.

### 2.7 예측 로직의 신뢰도
- AI 고장 예측 타이머는 최근 10개 포인트(약 10초)만 보고 선형회귀를 하기 때문에
  노이즈에 민감하다. 짧은 순간의 지터만으로도 "몇 분 후 위험" 예측값이 크게 출렁일 수 있다.
- 개선 방향: 지수평활(EMA)이나 더 긴 윈도우, 신뢰구간 표시를 추가해 "추정치"라는 점을
  UI에서도 더 명확히 알려야 한다(현재는 화면에는 안내 문구가 없고 코드 주석에만 있음).

### 2.8 경보 인지/알림 채널이 화면(시각)에만 의존
- 운전자가 화면을 보고 있지 않으면 위험 상황을 놓칠 수 있다. 소리 경보(Web Audio 비프음),
  나아가 실제 배포 시 SMS/사내 메신저 알림 연동을 고려해야 한다.

### 2.9 새로고침 시 상태 소실
- 브라우저를 새로고침하면 사고 이력, 임계값 설정이 모두 초기화된다. 데모용으로는 무방하지만
  실제 운영에는 서버 측 영속 저장소(DB)가 반드시 필요하다.

## 2.10 (신규) ESP32 실시간 연동의 한계
- `INGEST_API_KEY`가 ESP32 스케치 소스코드에 평문으로 박혀 있다. 보드를 분해/덤프하면 키가 노출될 수 있으므로,
  실제 배포 전에는 키 로테이션 절차나 디바이스별 개별 키 발급을 고려해야 한다.
- Redis에는 최신 값 1건만 덮어써서 저장한다(`hex-scada:latest`). 즉 서버 쪽에는 과거 이력이 전혀 남지 않고,
  프론트엔드가 폴링해서 쌓은 20개 포인트 히스토리도 브라우저를 새로고침하면 사라진다. 장기 이력이 필요하면
  타임시리즈 DB(예: InfluxDB, TimescaleDB)나 최소한 Redis List/Stream 구조로 바꿔야 한다.
- "5초 미수신 = 연결 끊김" 기준이 하드코딩되어 있다. 센서 개수가 늘거나 전송 주기를 바꾸면 이 값도 같이
  조정해야 하는데 지금은 설정 탭에서 변경할 수 없다.
- ESP32→Vercel 구간은 인증(API 키)만 있고 재시도/큐잉이 없다. 전송 실패 시 그 값은 그냥 유실된다.
  현장에서는 짧은 로컬 버퍼링(예: 최근 N개 실패분 재전송)을 추가하는 것이 안전하다.

## 3. 우선순위 제안

1순위(안전 직결): 2.3 물리적 인터락 분리, 2.2 타이머/백그라운드 탭 문제
2순위(운영 신뢰성): 2.1 실제 센서 연동, 2.9 영속 저장
3순위(거버넌스): 2.5 감사 추적, 2.4 다중 사용자 동기화
4순위(정교화): 2.6 센서 고장 탐지 고도화, 2.7 예측 신뢰도 표시, 2.8 알림 채널 다양화
